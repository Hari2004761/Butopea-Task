<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- List View -->
    <record id="estate_property_type_view_list" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <tree string="Property Types">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="offer_count" string="Offers"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="estate_property_type_view_form" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" name="action_view_offers"
                                type="object" icon="fa-money">
                            <field string="Offers" name="offer_count" widget="statinfo"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Type Name"/></h1>
                    </div>
                    <field name="property_ids">
                        <tree>
                            <field name="name"/>
                            <field name="expected_price"/>
                            <field name="state"/>
                        </tree>
                    </field>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="estate_property_type_view_kanban" model="ir.ui.view">
        <field name="name">estate.property.type.kanban</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <field name="offer_count"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click">
                            <div class="o_kanban_record_headings">
                                <strong class="o_kanban_record_title">
                                    <field name="name"/>
                                </strong>
                            </div>
                            <div class="o_kanban_record_body">
                                <t t-esc="record.offer_count.value"/> Offers
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Action -->
    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form,kanban</field>
    </record>

    <!-- Menu: loaded after estate_property_views.xml which defines estate_menu_properties -->
    <menuitem id="estate_menu_property_types"
              name="Property Types"
              parent="estate_menu_properties"
              action="estate_property_type_action"
              sequence="5"/>

</odoo>
